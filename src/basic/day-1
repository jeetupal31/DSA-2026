int[] marks = {78, 90, 65, 88, 72};

int[] arr = {10, 20, 30, 40, 50};

 ------------------------------------ NOTES------------------------
Array

Stores multiple values of same type.

Index starts from 0.

Access:
arr[index]

Traversal

for(int i=0;i<arr.length;i++)
{
    System.out.println(arr[i]);
}

---------------------------------------

int[] arr = {10, 20, 30, 40, 50};

System.out.println(arr[0]);

arr.length

int[] arr = {10, 20, 30, 40, 50};

System.out.println(arr.length);

for(int i = 0; i < arr.length; i ++){
System.out.println(arr[i]);
}
